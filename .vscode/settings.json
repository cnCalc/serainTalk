// 将设置放入此文件中以覆盖默认值和用户设置。
{
  // Configure glob patterns for excluding files and folders.  
  "files.exclude": {
    "**/.git": true,
    "**/.nyc_output": true,
    "**/coverage": true,
    // "**/uploads": true,
    // "**/database-convert": true,
    // "**/node_modules": true
  }
}
{
  "scripts": {
    "dev": "cross-env NODE_ENV=development webpack-dev-server --hot --host $(ip addr | grep -P 'inet ' | grep -vP '127.0.0.\\d{1,3}/8' | grep -Po '(\\d{1,3}\\.{0,1}){4}' | head -n 1)",
    "build": "cross-env NODE_ENV=production webpack --progress --hide-modules"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "pug": "^2.0.0-beta.12",
    "vue": "^2.2.1",
    "vue-router": "^2.5.2",
    "vuex": "^2.3.1",
    "vuex-router-sync": "^4.2.0"
  },
  "devDependencies": {
    "babel-core": "^6.0.0",
    "babel-loader": "^6.0.0",
    "babel-preset-latest": "^6.0.0",
    "cross-env": "^3.0.0",
    "css-loader": "^0.25.0",
    "file-loader": "^0.9.0",
    "node-sass": "^4.5.0",
    "sass-loader": "^5.0.1",
    "vue-loader": "^11.1.4",
    "vue-template-compiler": "^2.2.1",
    "webpack": "^2.2.0",
    "webpack-dev-server": "^2.2.0"
  }
}
